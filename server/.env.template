HOST=0.0.0.0
PORT=1337

# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
APP_KEYS="generated_key_1,generated_key_2"

# Generate with: openssl rand -base64 32
ADMIN_JWT_SECRET=generate_with_openssl_rand_base64_32
JWT_SECRET=generate_with_openssl_rand_base64_32
ENCRYPTION_KEY=generate_with_openssl_rand_base64_32

# Generate with: openssl rand -base64 16
API_TOKEN_SALT=generate_with_openssl_rand_base64_16
TRANSFER_TOKEN_SALT=generate_with_openssl_rand_base64_16

# CORS — URL of the frontend client
CLIENT_URL=http://localhost:5173

# Google Gemini API key — https://aistudio.google.com/app/apikey
GEMINI_API_KEY=your_google_gemini_api_key